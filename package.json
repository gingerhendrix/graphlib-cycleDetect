{
  "name": "graphlib-cycle-detect",
  "version": "1.0.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "test": "npm run compile && mocha -R spec --compilers js:babel-register",
    "compile": "babel --presets es2015 -d lib/ src/",
    "prepublish": "npm run compile"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-preset-es2015": "^6.6.0",
    "babel-register": "^6.7.2",
    "expect.js": "^0.3.1",
    "mocha": "^2.4.5"
  },
  "dependencies": {
    "graphlib": "^2.1.0"
  }
}
